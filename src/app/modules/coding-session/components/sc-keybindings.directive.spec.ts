import { ScKeybindingsDirective } from './sc-keybindings.directive';

describe('ScKeybindingsDirective', () => {
  it('should create an instance', () => {
    const directive = new ScKeybindingsDirective();
    expect(directive).toBeTruthy();
  });
});
